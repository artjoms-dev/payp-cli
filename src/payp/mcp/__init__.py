"""MCP server for payp — exposes payp tools to external MCP clients.

Usage:
    python -m payp.mcp.server

External MCP clients (Claude Desktop, Cursor, etc.) can connect via stdio
to access payp's database tools with the same safety layers.
"""
